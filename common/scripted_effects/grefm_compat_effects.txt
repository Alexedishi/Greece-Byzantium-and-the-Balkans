# Right now, there is only intentional compatibility with MGR.
# This effect will set variables to enable alternate content or knock out incompatible content in cases where using global triggers is overkill

grefm_compatibility_check = { 
	if = {
		limit = {
			morgenrote_is_active = yes
		}
		set_global_variable = grefm_morgenrote_var
	}
}