patriarch_authority_capacity = {
	value = 0
## BASE	
	add = 100
## SOURCES
	# Law Check
	if = {
		limit = {
			has_law = law_type:law_theocracy
		}
		add = 200
	}
	else_if = {
		limit = {
			has_law = law_type:law_monarchy
		}
		add = 100
	}
	if = {
		limit = {
			has_law = law_type:law_state_religion
		}
		add = 200
	}
	else_if = {
		limit = {
			has_law = law_type:law_freedom_of_conscience
		}
		add = 100
	}
	if = {
		limit = {
			has_variable = eocfm_is_autocephalous
		}
		add = 200
	}
	
	# Modifiers Check
	if = {
		limit = {
			has_modifier = modifier_eocfm_caesaropapism
		}
		add = 100
	}
	if = {
		limit = {
			has_modifier = modifier_eocfm_subsidizing_church
		}
		add = 100
	}
	
	# Patriarchal Seats Check
	if = {
		limit = {
			eocfm_controls_all_seats = yes
		}
		add = 400
	}
	if = {
		limit = {
			has_variable = eocfm_controls_rome
		}
		add = 100
	}
	if = {
		limit = {
			has_variable = eocfm_controls_constantinople
		}
		add = 100
	}
	if = {
		limit = {
			has_variable = eocfm_controls_antioch
		}
		add = 100
	}
	if = {
		limit = {
			has_variable = eocfm_controls_alexandria
		}
		add = 100
	}
	if = {
		limit = {
			has_variable = eocfm_controls_jerusalem
		}
		add = 100
	}
	if = {
		limit = {
			has_variable = eocfm_controls_moscow
		}
		add = 100
	}

## EXPENSES
	# Major Actions Check
	if = {
		limit = {
			has_modifier = modifier_eocfm_hosting_council
		}
		subtract = 1000
	}
	if = {
		limit = {
			has_modifier = modifier_eocfm_integrating_seat
		}
		subtract = 500
	}
	if = {
		limit = {
			has_journal_entry = je_eocfm_autocephaly
		}
		subtract = 800
	}
	
	# Law Check
	if = {
		limit = {
			has_law = law_type:law_total_separation
		}
		subtract = 100
	}
	
	# Modifiers Check
	if = {
		limit = {
			eocfm_has_icon_trigger = yes
		}
		subtract = 200
	}
}